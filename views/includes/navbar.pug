nav.navbar.navbar-inverse
  .container
    .navbar-header
      button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#navbar', aria-expanded='false', aria-controls='navbar')
        span.sr-only Toggle navigation
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href='/')
        span.drinkin-color Drink
        | In

    #navbar.navbar-collapse.collapse
      ul.nav.navbar-nav
        li(class=(title === 'Menu' ? 'active' : '') )
          a(href='/#/menu') Menu
        li(class=(title === 'Dishes' ? 'active' : '') )
          a(href='/admin/dishes') Dishes
        li( class=(title === 'Categories' ? 'active' : '') )
          a(href='/admin/categories') Categories
        li(class=(title === 'Allergens' ? 'active' : '') )
          a(href='/admin/allergens') Allergens
        if (!user)
          li(class=(title === 'SignUp' ? 'active' : '') )
            a(href='/local/login') LogIn
          li(class=(title === 'LogIn' ? 'active' : '') )
            a(href='/local/register') SignUp
        if (user)
          li(class=(title === 'Logout' ? 'active' : '') )
            a(href='/') Logout
